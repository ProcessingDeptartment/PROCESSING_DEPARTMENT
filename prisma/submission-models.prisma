
// ============================================================================
// AUTO-GENERATED per-record submission tables.
// Generated by scripts/generate-submission-schema.mjs — do not edit by hand.
// ============================================================================

model Sub_dry_export_pack_front_page {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  packingDate              String?
  noOfBoxesExported        String?
  rec747Attached           String?
  rec748Attached           String?
  rec749Attached           String?
  signedPackingListsAttached String?
  healthCertificatesAttached String?
  completedBySupervisor    String?
  checkedByQC              String?
  verificationBy           String?

  @@map("sub_dry_export_pack_front_page")
}

model Sub_live_leftovers_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  sizeClass                String?
  weight                   String?

  @@map("sub_live_leftovers_log")
}

model Sub_master_cleaning_checklist {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  weekOf                   String?
  responsibleOperator      String?

  rows Sub_master_cleaning_checklist_Row[]

  @@map("sub_master_cleaning_checklist")
}

model Sub_master_cleaning_checklist_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_master_cleaning_checklist @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  equipmentOrItem          String?
  frequency                String?
  day                      String?
  shift                    String?
  done                     String?

  @@index([parentId])
  @@map("sub_master_cleaning_checklist_row")
}

model Sub_master_cleaning_plan {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  effectiveDate            String?
  approvedBy               String?

  rows Sub_master_cleaning_plan_Row[]

  @@map("sub_master_cleaning_plan")
}

model Sub_master_cleaning_plan_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_master_cleaning_plan @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  partToBeCleaned          String?
  where                    String?
  frequency                String?
  cleaningSupplies         String?
  how                      String?
  who                      String?

  @@index([parentId])
  @@map("sub_master_cleaning_plan_row")
}

model Sub_mortalities_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  mortalitiesKg            String?
  totalPack                String?
  percent                  String?

  @@map("sub_mortalities_log")
}

model Sub_mortality_counts_by_customer {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?

  rows Sub_mortality_counts_by_customer_Row[]

  @@map("sub_mortality_counts_by_customer")
}

model Sub_mortality_counts_by_customer_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_mortality_counts_by_customer @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  customer                 String?
  sizeRange                String?
  noMortalities            String?

  @@index([parentId])
  @@map("sub_mortality_counts_by_customer_row")
}

model Sub_production_information_nrcs {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  receivingDate            String?
  dateOfInspection         String?
  productionCode           String?
  nrcsAgCode               String?
  productDescription       String?
  checkedByQC              String?
  verificationBy           String?

  rows Sub_production_information_nrcs_Row[]

  @@map("sub_production_information_nrcs")
}

model Sub_production_information_nrcs_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_production_information_nrcs @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  recordName               String?
  recordCode               String?
  included                 String?

  @@index([parentId])
  @@map("sub_production_information_nrcs_row")
}

model Sub_dry_nrcs_packs {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  receivingDate            String?
  dateOfInspection         String?
  jobNumberProductionDate  String?
  nrcsAgCode               String?
  productDescription       String?
  checkedByQC              String?
  verificationBy           String?

  rows Sub_dry_nrcs_packs_Row[]

  @@map("sub_dry_nrcs_packs")
}

model Sub_dry_nrcs_packs_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_dry_nrcs_packs @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  recordName               String?
  recordCode               String?
  included                 String?

  @@index([parentId])
  @@map("sub_dry_nrcs_packs_row")
}

model Sub_production_information_nrcs_rework {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  reworkDate               String?
  dateOfInspection         String?
  productionCode           String?
  nrcsAgCode               String?
  productDescription       String?
  reasonsForRework         String?
  checkedByQC              String?
  verificationBy           String?

  rows Sub_production_information_nrcs_rework_Row[]

  @@map("sub_production_information_nrcs_rework")
}

model Sub_production_information_nrcs_rework_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_production_information_nrcs_rework @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  recordName               String?
  recordCode               String?
  included                 String?

  @@index([parentId])
  @@map("sub_production_information_nrcs_rework_row")
}

model Sub_qa1_damaged_cans_and_lids {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  noOfCansEnds             String?
  palletNumberProductionDate String?
  supervisor               String?
  checkedByQC              String?

  @@map("sub_qa1_damaged_cans_and_lids")
}

model Sub_cans_released_form {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  noOfPieces               String?
  brineOrBraised           String?
  noOfGrams                String?
  noOfCans                 String?
  agCode                   String?
  checkedByQC              String?

  @@map("sub_cans_released_form")
}

model Sub_gonad_inspection_report {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiIntakeWeight           String?
  processingFor            String?
  date                     String?
  stage                    String?
  sizeRange                String?
  sex                      String?
  damagesAfterGutting      String?
  basketReceivingComments  String?
  checkedBy                String?

  @@map("sub_gonad_inspection_report")
}

model Sub_training_register {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  trainingCourse           String?
  date                     String?
  documentNumbers          String?
  duration                 String?
  trainer                  String?

  rows Sub_training_register_Row[]

  @@map("sub_training_register")
}

model Sub_training_register_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_training_register @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  nameSurname              String?
  signature                String?
  employeeNumber           String?
  competencyConfirmed      String?
  carNo                    String?

  @@index([parentId])
  @@map("sub_training_register_row")
}

model Sub_incubator_cans_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  dateIn                   String?
  productionCode           String?
  productDescription       String?
  pieces                   String?
  quantity                 String?
  checkedBy                String?
  dateOutForMicro          String?
  qtyOutForTesting         String?
  checkedByMicro           String?
  productCleared           String?
  notClearedReason         String?
  dateCleared              String?
  clearedByName            String?

  @@map("sub_incubator_cans_log")
}

model Sub_fixed_reader_checks {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  location                 String?
  startTime                String?
  endTime                  String?
  itemNo                   String?
  rfidTagNumber            String?
  qdeMatch                 String?
  comment                  String?

  @@map("sub_fixed_reader_checks")
}

model Sub_basket_removal_shucking_gutting {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivedFrom           String?
  jiIntakeWeight           String?
  intakeDate               String?
  processingFor            String?
  sizeRange                String?
  incomingTemp             String?
  noOfMortalities          String?
  signsOfParasites         String?
  foreignObjects           String?
  damagesOnFoot            String?
  damagesAfterShucking     String?
  beakCutCorrect           String?
  damagesAfterGutting      String?
  processDeviations        String?

  @@map("sub_basket_removal_shucking_gutting")
}

model Sub_abalone_receiving {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  receivingDate            String?
  receivedFrom             String?
  toBeProcessedFor         String?
  intakeWeight             String?

  rows Sub_abalone_receiving_Row[]

  @@map("sub_abalone_receiving")
}

model Sub_abalone_receiving_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_abalone_receiving @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  sizeRange                String?
  basketNr                 String?
  wholeWeight              String?
  farmCount                String?
  mortalityCount           String?

  @@index([parentId])
  @@map("sub_abalone_receiving_row")
}

model Sub_salting_and_tumbling {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  intakeDate               String?
  harvestFarm              String?
  jiIntakeWeight           String?
  date                     String?
  processingFor            String?
  qualityController        String?
  processDeviation         String?

  rows Sub_salting_and_tumbling_Row[]

  @@map("sub_salting_and_tumbling")
}

model Sub_salting_and_tumbling_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_salting_and_tumbling @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  batchId                  String?
  sizeRange                String?
  standardSaltingTime      String?
  noOfCrates               String?
  startTime                String?
  finishTime               String?
  totalTumblingTime        String?
  shuckWeight              String?
  saltKg                   String?
  saltPercentUsed          String?
  saltBatchCode            String?
  bakingSodaKg             String?
  bakingSodaBatchCode      String?
  sugarKg                  String?
  sugarPercentUsed         String?
  sugarBatchCode           String?

  @@index([parentId])
  @@map("sub_salting_and_tumbling_row")
}

model Sub_bleeding_and_salting {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiIntakeWeight           String?
  processingFor            String?
  date                     String?
  sizeRange                String?
  noOfCrates               String?
  bleedingStartTime        String?
  bleedingFinishTime       String?
  bleedingTotalTime        String?
  standardSaltingTime      String?
  saltingStartTime         String?
  saltingFinishTime        String?
  saltingTotalTime         String?
  shuckWeight              String?
  saltKg                   String?
  saltPercent              String?
  saltBatchCode            String?
  bakingSodaG              String?
  bakingSodaBatchCode      String?
  qualityController        String?
  processDeviation         String?

  @@map("sub_bleeding_and_salting")
}

model Sub_washing_control_sheet {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  timeStarted              String?
  washNumber               String?
  sizeRange                String?
  machineNo                String?
  binsTraysPerWash         String?
  totalWashingTime         String?
  abaloneCleanAfterWash    String?
  qcOnline                 String?
  processDeviation         String?

  @@map("sub_washing_control_sheet")
}

model Sub_salting_oosw {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiIntakeWeight           String?
  intakeDate               String?
  harvestFarm              String?
  processingFor            String?
  sizeRange                String?
  wholeWeight              String?
  supervisor               String?
  supervisorTitle          String?
  supervisorDate           String?
  comments                 String?

  rows Sub_salting_oosw_Row[]

  @@map("sub_salting_oosw")
}

model Sub_salting_oosw_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_salting_oosw @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  weight                   String?

  @@index([parentId])
  @@map("sub_salting_oosw_row")
}

model Sub_scrubbing_check_supervisor {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  abaloneSize              String?
  date                     String?
  time                     String?
  productCleanedEffectively String?
  damagedAbalone           String?
  weightDamagedAbalone     String?
  weightDamagedTrimmed     String?
  supervisor               String?

  @@map("sub_scrubbing_check_supervisor")
}

model Sub_ph_verification {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  meterSerial              String?
  buffer4Reading           String?
  buffer7Reading           String?
  buffer10Reading          String?
  checkedBy                String?
  comments                 String?

  @@map("sub_ph_verification")
}

model Sub_thermometer_verification {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  referenceThermoSerial    String?
  testThermoSerial         String?
  coldReferenceReading     String?
  coldTestReading          String?
  coldDeviation            String?
  hotReferenceReading      String?
  hotTestReading           String?
  hotDeviation             String?
  checkedBy                String?
  verifiedBy               String?
  verifiedDate             String?

  @@map("sub_thermometer_verification")
}

model Sub_thermometer_correction_factors {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  month                    String?
  date                     String?
  referenceThermoSerial    String?
  referenceReading         String?
  testThermoSerial         String?
  testReading              String?
  difference               String?
  correctionUsed           String?
  checkedBy                String?

  @@map("sub_thermometer_correction_factors")
}

model Sub_sampling_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  productInfo              String?
  pieces                   String?
  quantity                 String?
  reason                   String?
  sampledBy                String?

  @@map("sub_sampling_log")
}

model Sub_stock_loading {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  agCode                   String?
  canPieces                String?
  drainedWeight            String?
  sauceType                String?
  numberOfCans             String?
  commentsDents            String?
  checkedBy                String?

  @@map("sub_stock_loading")
}

model Sub_qc_report {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivingDate          String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  harvestFarm              String?
  date                     String?
  productionCode           String?
  productDescription       String?
  canPieces                String?
  nettMassMin              String?
  drainMassMin             String?
  drainMassMax             String?
  ph                       String?
  brix                     String?
  waterReading             String?
  saltPercent              String?
  vacuum                   String?
  rustOnCans               String?
  rustOnLids               String?
  dentsOnBatch             String?
  oddOursOnOpening         String?
  damagedPcsInCans         String?
  bluingBlackSpots         String?
  sauceColour              String?
  visualAppearance         String?
  textureOfAbalone         String?
  tasteOfProduct           String?
  comments                 String?

  @@map("sub_qc_report")
}

model Sub_double_seam_inspection_report {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?


  @@map("sub_double_seam_inspection_report")
}

model Sub_rework_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  reasonForRework          String?
  reworkApprovalBy         String?
  productDescriptionDw     String?
  productionDateOrFg01TransferDate String?
  quantityCans             String?
  newProductionDate        String?
  quantityProduced         String?
  checkedByQC              String?

  @@map("sub_rework_log")
}

model Sub_product_label_checklist {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  supplier                 String?
  labelDescription         String?
  labelVersion             String?
  quantityOrdered          String?
  quantityReceived         String?
  generalConditionAtReceiving String?
  labelDimensionsCorrect   String?
  artworkCorrect           String?
  ingredientStatementCorrect String?
  allergenDeclarationComplies String?
  labelCompliesApproved    String?
  receivedBy               String?
  comments                 String?

  @@map("sub_product_label_checklist")
}

model Sub_labelling_of_cans {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  labelUsed                String?
  stickersUsed             String?
  clientDetails            String?
  quantityLabelsIssued     String?
  quantityIssued           String?
  boxesFull                String?
  cansCheckedBeforeLabelling String?
  cansCheckedAfterLabelling String?
  correctLabelUsed         String?
  labellingRequirementCorrect String?
  cartonLabelsCorrespond   String?
  correctiveActions        String?

  @@map("sub_labelling_of_cans")
}

model Sub_stock_transfers {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  agCode                   String?
  qualitySignOff           String?
  numberOfBoxes            String?
  pieces                   String?
  drainedWeight            String?
  sauceType                String?
  orderDesignation         String?
  numberOfCans             String?
  qcVerification           String?
  fg02Verification         String?
  fg01Verification         String?

  @@map("sub_stock_transfers")
}

model Sub_scrubbing_checklist_qc {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  time                     String?
  sizeRange                String?
  noOfCratesInspected      String?
  damagedAbalone           String?
  allScrubbedClean         String?
  acceptableDamageTrimmed  String?
  processDeviation         String?
  checkedBy                String?

  @@map("sub_scrubbing_checklist_qc")
}

model Sub_precooking_check_sheet {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  precookingDate           String?
  sizeRange                String?
  litresWaterPerCook       String?
  sugarKgUsed              String?
  sugarBatchCode           String?
  precookingStartTime      String?
  temperature              String?
  waterInPotChanged        String?
  rinseWaterChanged        String?
  cleanWeight              String?
  precookWeight            String?
  totalWholeWeight         String?
  totalCleanWeight         String?
  totalPrecookWeight       String?
  percentCleanWeight       String?
  percentPrecookWeight     String?

  @@map("sub_precooking_check_sheet")
}

model Sub_abalone_packing_specification {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  jobNo                    String?
  pcs                      String?
  drainedWeight            String?
  ingoWeight               String?
  minSpec                  String?
  maxSpec                  String?
  cookoutPct               String?
  newMinIngo               String?
  newMaxIngo               String?
  comments                 String?

  @@map("sub_abalone_packing_specification")
}

model Sub_can_packing_control_sheet {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  time                     String?
  sizeRange                String?
  qualityOfAbalone         String?
  piecesSizes              String?
  ingoWeightSpec           String?
  actualIngoWeight         String?
  qcOnline                 String?
  processDeviation         String?

  @@map("sub_can_packing_control_sheet")
}

model Sub_can_filling_and_printing {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  startTime                String?
  waterOrSauceFillingBatch String?
  productTemp              String?
  nettWeightSpec           String?
  actualNettWeight         String?
  printedAgCode            String?
  printedBestBeforeDate    String?
  verifiedBeforePrinting   String?
  verifiedAfterPrinting    String?
  processDeviations        String?

  @@map("sub_can_filling_and_printing")
}

model Sub_printing_control_sheet {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  productionDate           String?
  time                     String?
  codeCorrect              String?
  canPosition              String?
  actualCanPieces          String?
  piecesOnCan              String?
  correctiveActions        String?

  @@map("sub_printing_control_sheet")
}

model Sub_cans_produced {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  intakeDate               String?
  productionCode           String?
  timeTrolleyIn            String?
  retortCode               String?
  retortNo                 String?
  cookingMethodRecipe      String?
  canPieces                String?
  drainWeight              String?
  brineOrBraised           String?
  sauceBatch               String?
  numberOfCans             String?
  damagedCans              String?
  reasonForDamage          String?
  totalDamagedCans         String?

  @@map("sub_cans_produced")
}

model Sub_retorting_control_sheet {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  time                     String?
  productionCode           String?
  productRecipe            String?
  timeTrolleyIn            String?
  retortNo                 String?
  pressureOnGauge          String?
  tempOnGauge              String?
  pressureOnScreen         String?
  tempOnScreen             String?
  startSterilization       String?
  startCooling             String?
  timeTrolleyOut           String?
  processDeviation         String?

  @@map("sub_retorting_control_sheet")
}

model Sub_retort_inspection_report {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  intakeDate               String?
  harvestFarm              String?
  retortCode               String?
  timeTrolleyIn            String?
  product                  String?
  canningCode              String?
  canSize                  String?
  retortDoorRubberSprayed  String?
  preHeatCycleDone         String?
  airFilterOk              String?
  stackingPatternCorrect   String?
  recipeUsed               String?
  autoclaveTapeOnTrolley   String?
  correctTempOnRecipe      String?
  correctPressureOnRecipe  String?
  processDeviationAlarms   String?

  @@map("sub_retort_inspection_report")
}

model Sub_broth_cooking {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  potNo                    String?
  waterLitres              String?
  abaloneShellsIntakeDate  String?
  driedKelpWeight          String?
  scallopJuiceWeight       String?
  finalBrothLitres         String?
  ph                       String?
  salt                     String?
  brix                     String?
  cookedBy                 String?
  comments                 String?

  @@map("sub_broth_cooking")
}

model Sub_ingredient_weighing {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  chickenFlavourWeight     String?
  chickenFlavourBatchCode  String?
  vegetableBaseWeight      String?
  vegetableBaseBatchCode   String?
  gelatineWeight           String?
  gelatineBatchCode        String?
  xanthanGumWeight         String?
  xanthanGumBatchCode      String?
  maltSugarWeight          String?
  maltSugarBatchCode       String?
  tapiocaStarchWeight      String?
  tapiocaStarchBatchCode   String?
  brownSugarWeight         String?
  brownSugarBatchCode      String?
  sherloneSauceWeight      String?
  sherloneSauceBatchCode   String?
  weighedBy                String?
  comments                 String?

  @@map("sub_ingredient_weighing")
}

model Sub_sauce_mixing {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  brothBatchDate           String?
  potNo                    String?
  litres                   String?
  phBroth                  String?
  brixBroth                String?
  saltBroth                String?
  chickenLiquidWeight      String?
  chickenLiquidBatchCode   String?
  smokeyFlavourWeight      String?
  smokeyFlavourBatchCode   String?
  cvgMixBatchCode          String?
  xg1BatchCode             String?
  xg2BatchCode             String?
  xg3BatchCode             String?
  mdMixBatchCode           String?
  tsMixBatchCode           String?
  soMixBatchCode           String?

  @@map("sub_sauce_mixing")
}

model Sub_final_sauce_mix {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  potNo                    String?
  batchCodeSauce           String?
  ph                       String?
  salt                     String?
  brix                     String?
  saltAdjustment           String?
  brownSugarAdjustment     String?
  maltAdjustment           String?
  comments                 String?

  @@map("sub_final_sauce_mix")
}

model Sub_sauce_batch_coding {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  dateOfMix                String?
  potCode                  String?
  bucketNumber             String?
  weightOfBucket           String?
  supervisor               String?
  comments                 String?

  @@map("sub_sauce_batch_coding")
}

model Sub_brine_mixing_report {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  sample                   String?
  productDescription       String?
  productionDateIngo       String?
  canPieces                String?
  nettWeightMin            String?
  drainMassMin             String?
  drainMassMax             String?
  saltG                    String?
  saltBatchNo              String?
  saltPercent              String?
  sugarG                   String?
  sugarBatchNo             String?
  sugarPercent             String?
  maltG                    String?
  maltBatchNo              String?
  commentsDeviations       String?

  @@map("sub_brine_mixing_report")
}

model Sub_dry_cooking {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  harvestFarm              String?
  intakeDate               String?
  cookingDate              String?
  potNumber                String?
  abaloneKg                String?
  blanchSeaWater           String?
  blanchPh                 String?
  blanchSaltKg             String?
  blanchTemp               String?
  cookingTime              String?
  blanchBatchNumber        String?
  startTime                String?
  startingTemp             String?
  temp20MinAfter           String?
  endOfCookingTemp         String?
  timeOut                  String?
  totalCookingTime         String?
  cookSeaWater             String?
  cookPh                   String?
  saltKg                   String?
  saltBatchNumber          String?
  sugarKg                  String?
  sugarBatchNumber         String?
  vinegarKg                String?
  vinegarBatchNumber       String?
  cooker                   String?
  comments                 String?

  @@map("sub_dry_cooking")
}

model Sub_drying_process {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  wholeWeight              String?
  cookingDate              String?
  cookingWeight            String?
  movementToDryRoom1Date   String?
  steam1Date               String?
  steam2Date               String?
  steam3Date               String?
  steam4Date               String?
  steam5Date               String?
  steam6Date               String?
  steam7Date               String?
  steam8Date               String?
  steam9Date               String?
  steam10Date              String?
  steam11Date              String?
  steam12Date              String?
  steam13Date              String?
  steam14Date              String?
  movementToContainerDate  String?
  removedFromTrolleysDate  String?
  movementToGradingRoomDate String?
  dateDeString             String?
  dateGraded               String?
  totalDryingTimeDays      String?
  dryWeight                String?
  estimateYield            String?

  @@map("sub_drying_process")
}

model Sub_dried_abalone_transfer {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  intakeDate               String?
  intakeWeight             String?
  actualDriedDate          String?
  totalDriedWeight         String?
  dryYield                 String?
  receivedBy               String?
  verifiedBy               String?

  rows Sub_dried_abalone_transfer_Row[]

  @@map("sub_dried_abalone_transfer")
}

model Sub_dried_abalone_transfer_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_dried_abalone_transfer @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  crateNumber              String?
  weight                   String?

  @@index([parentId])
  @@map("sub_dried_abalone_transfer_row")
}

model Sub_dry_monitoring {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  intakeWeight             String?
  intakeDate               String?
  date                     String?
  cookingDate              String?
  dryRoomArea              String?
  noOfTrolleys             String?
  trolleysClearlyMarked    String?
  estimatedDryDate         String?
  mouldVisible             String?
  whiteSaltOnSurface       String?
  caseHardening            String?
  surfaceShine             String?
  footDamage               String?
  correctiveActions        String?
  comments                 String?
  qcCheck                  String?
  supervisor               String?

  @@map("sub_dry_monitoring")
}

model Sub_grading_production_log_cultivated {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  intakeDate               String?
  driedWeightReceived      String?
  actualDriedWeight        String?
  dateIntoGradingRoom      String?
  gradingDate              String?
  under8g                  String?
  g8to10                   String?
  g11to13                  String?
  g14to16                  String?
  g17to20                  String?
  g21to23                  String?
  g24to25                  String?
  g26to28                  String?
  g29to31                  String?
  g32to35                  String?
  g36to39                  String?
  g40to44                  String?
  g45to50                  String?
  g51to55                  String?
  g56to60                  String?
  g60plus                  String?
  bGrade                   String?
  comments                 String?
  actualGradedWeight       String?
  yield                    String?
  nameOfGraders            String?
  supervisor               String?
  verifiedBy               String?

  rows Sub_grading_production_log_cultivated_Row[]

  @@map("sub_grading_production_log_cultivated")
}

model Sub_grading_production_log_cultivated_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_grading_production_log_cultivated @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  binCode                  String?
  binWeightStart           String?
  fullBoxWeight            String?
  finalBinWeight           String?
  gradedWeight             String?

  @@index([parentId])
  @@map("sub_grading_production_log_cultivated_row")
}

model Sub_grading_production_log_ranched {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  intakeDate               String?
  driedWeightReceived      String?
  actualDriedWeight        String?
  dateIntoGradingRoom      String?
  gradingDate              String?
  under50g                 String?
  g51to80                  String?
  g81to100                 String?
  g101to150                String?
  g151to200                String?
  g200plus                 String?
  darkUnder100g            String?
  dark100gPlus             String?
  bGrade                   String?
  comments                 String?
  actualGradedWeight       String?
  yield                    String?
  nameOfGraders            String?
  supervisor               String?
  verifiedBy               String?

  rows Sub_grading_production_log_ranched_Row[]

  @@map("sub_grading_production_log_ranched")
}

model Sub_grading_production_log_ranched_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_grading_production_log_ranched @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  binCode                  String?
  binWeightStart           String?
  fullBoxWeight            String?
  finalBinWeight           String?
  gradedWeight             String?

  @@index([parentId])
  @@map("sub_grading_production_log_ranched_row")
}

model Sub_grading_boxing_traceability {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  dateCreated              String?
  binCode                  String?
  sizeGrade                String?
  jobNumbersInBin          String?
  supervisor               String?

  @@map("sub_grading_boxing_traceability")
}

model Sub_boxing_and_labelling {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  packingDate              String?
  tareWeight               String?
  binCode                  String?
  sizeRange                String?
  colourUniform            String?
  correctSizeGrade         String?
  frillsPresent            String?
  bagAndBoxSealed          String?
  nettWeight               String?
  qualityController        String?
  supervisor               String?

  @@map("sub_boxing_and_labelling")
}

model Sub_dry_stock_control {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  intakeDate               String?
  month                    String?
  wholeWeight              String?
  cookingDate              String?
  noOfTrolleys             String?
  weightIn                 String?
  dateIn                   String?
  countInFactory           String?
  countInFinance           String?
  weightOut                String?
  dateOut                  String?
  countOutFactory          String?
  countOutFinance          String?

  @@map("sub_dry_stock_control")
}

model Sub_labelling_of_dry_boxes {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  clientName               String?
  clientAddress            String?
  quantityOfBoxes          String?
  productDescription       String?
  agCodeMatchesInspection  String?
  sizeRangeCorrectAcrossBoxes String?
  boxCountMatchesPackingList String?
  addressMatchesPackingList String?
  correctiveActions        String?
  inspectedByProcessing    String?
  inspectedByQC            String?
  inspectedByFG01          String?

  @@map("sub_labelling_of_dry_boxes")
}

model Sub_dry_labelling_list {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  customer                 String?
  flight                   String?
  address                  String?
  airwaybillNr             String?
  etd                      String?
  nettWeight               String?
  salesDept                String?
  driverFromFreightCo      String?
  abagoldEmployee          String?

  rows Sub_dry_labelling_list_Row[]

  @@map("sub_dry_labelling_list")
}

model Sub_dry_labelling_list_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_dry_labelling_list @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  boxNo                    String?
  gradeRange               String?
  boxCode                  String?
  agCode                   String?
  weight                   String?

  @@index([parentId])
  @@map("sub_dry_labelling_list_row")
}

model Sub_dry_stock_transfers {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  noOfBoxes                String?
  timeOfTransfer           String?
  transferredBy            String?
  receivedBy               String?

  @@map("sub_dry_stock_transfers")
}

model Sub_live_production_pack {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  packingDate              String?
  idnsPacked               String?
  livePackChecklistAttached String?
  purgeStartDate           String?
  purgeEndDate             String?
  purgeDays                String?
  bagsIntoPurge            String?
  massIntoPurge            String?
  massOutOfPurge           String?
  purgeLoss                String?
  bagsPacked               String?
  boxesPacked              String?
  massPacked               String?
  oxygenBatchNumber        String?
  spongesBatchNumber       String?
  bagsBoxFlatBoardBatchNumber String?
  icePacksBatchNumber      String?
  signedPackingListsAttached String?
  completedBySupervisor    String?
  checkedByQC              String?

  @@map("sub_live_production_pack")
}

model Sub_live_pack_checklist {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  customerIdn              String?
  productionDate           String?
  numberOfCartons          String?
  dripTimeOfBags           String?
  startTime                String?
  scaleTareWeight          String?
  waterTemp                String?
  cleaningInspectionBefore String?
  mortalitiesWhilePacking  String?
  noOfMortalities          String?
  damagesOnAbalone         String?
  shellsCleanedWhite       String?
  signsOfParasites         String?
  allBoxesHaveSponges      String?
  noOfIcepacksPerBox       String?
  gasInjectedInEachBox     String?
  boxesSealedAndLabelled   String?
  timeFinishedPacking      String?
  allBoxesLoadedInTruck    String?
  truckLockedSealed        String?
  cleaningInspectionAfter  String?
  leftoversSizeClass       String?
  leftoversWeight          String?
  supervisor               String?

  @@map("sub_live_pack_checklist")
}

model Sub_live_packing_bag_quality_check {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  dateOfChecking           String?
  dateOfPacking            String?
  noOfBagsScheduled        String?
  noOfBagsToBeChecked      String?
  waterTemperature         String?
  totalMortalityWeight     String?
  additionalComments       String?
  supervisorFarm           String?
  supervisorProcessing     String?
  qcProcessing             String?
  verifiedBy               String?

  rows Sub_live_packing_bag_quality_check_Row[]

  @@map("sub_live_packing_bag_quality_check")
}

model Sub_live_packing_bag_quality_check_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_live_packing_bag_quality_check @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  client                   String?
  idn                      String?
  dateSorted               String?
  qualityOfBags            String?
  tankNumber               String?
  bagsInTank               String?
  mortalitiesRemoved       String?
  mortalitiesQuantity      String?

  @@index([parentId])
  @@map("sub_live_packing_bag_quality_check_row")
}

model Sub_individual_abalone_weight_checks {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  shift                    String?
  checkedByQC              String?
  qaVerification           String?

  rows Sub_individual_abalone_weight_checks_Row[]

  @@map("sub_individual_abalone_weight_checks")
}

model Sub_individual_abalone_weight_checks_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_individual_abalone_weight_checks @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  nameOfGrader             String?
  nameOfCapturer           String?
  nameOfSupervisor         String?
  bagNumber                String?
  sizeRange                String?
  gradeRange               String?
  individualWeights        String?
  approved                 String?

  @@index([parentId])
  @@map("sub_individual_abalone_weight_checks_row")
}

model Sub_receiving_live_returns {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  clientIdn                String?
  boxNo                    String?
  sizeRange                String?
  count                    String?
  basketNumberTrackingNo   String?
  positionNo               String?
  supervisor               String?

  @@map("sub_receiving_live_returns")
}

model Sub_production_areas_cleaning_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  shift                    String?
  inspectedBy              String?
  verifiedBy               String?

  rows Sub_production_areas_cleaning_record_Row[]

  @@map("sub_production_areas_cleaning_record")
}

model Sub_production_areas_cleaning_record_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_production_areas_cleaning_record @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  area                     String?
  item                     String?
  val_status               String?
  nonConformance           String?
  correctionSignOff        String?

  @@index([parentId])
  @@map("sub_production_areas_cleaning_record_row")
}

model Sub_live_product_areas_cleaning_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  shift                    String?
  inspectedBy              String?
  verifiedBy               String?

  rows Sub_live_product_areas_cleaning_record_Row[]

  @@map("sub_live_product_areas_cleaning_record")
}

model Sub_live_product_areas_cleaning_record_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_live_product_areas_cleaning_record @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  area                     String?
  item                     String?
  val_status               String?
  nonConformance           String?
  correctionSignOff        String?

  @@index([parentId])
  @@map("sub_live_product_areas_cleaning_record_row")
}

model Sub_personnel_facilities_cleaning_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  shift                    String?
  inspectedBy              String?
  verifiedBy               String?

  rows Sub_personnel_facilities_cleaning_record_Row[]

  @@map("sub_personnel_facilities_cleaning_record")
}

model Sub_personnel_facilities_cleaning_record_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_personnel_facilities_cleaning_record @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  area                     String?
  item                     String?
  val_status               String?
  nonConformance           String?
  correctionSignOff        String?

  @@index([parentId])
  @@map("sub_personnel_facilities_cleaning_record_row")
}

model Sub_daily_cleaning_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  shift                    String?

  rows Sub_daily_cleaning_inspection_Row[]

  @@map("sub_daily_cleaning_inspection")
}

model Sub_daily_cleaning_inspection_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_daily_cleaning_inspection @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  area                     String?
  item                     String?
  val_status               String?
  nonConformance           String?
  correctionSignOff        String?

  @@index([parentId])
  @@map("sub_daily_cleaning_inspection_row")
}

model Sub_deep_cleaning_record_weekly {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  shift                    String?

  rows Sub_deep_cleaning_record_weekly_Row[]

  @@map("sub_deep_cleaning_record_weekly")
}

model Sub_deep_cleaning_record_weekly_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_deep_cleaning_record_weekly @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  area                     String?
  item                     String?
  val_status               String?
  finding                  String?
  correction               String?
  signOff                  String?

  @@index([parentId])
  @@map("sub_deep_cleaning_record_weekly_row")
}

model Sub_weekly_cleaning_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  weekOf                   String?
  checkedBy                String?
  supervisor               String?

  rows Sub_weekly_cleaning_record_Row[]

  @@map("sub_weekly_cleaning_record")
}

model Sub_weekly_cleaning_record_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_weekly_cleaning_record @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  areaEquipment            String?
  done                     String?
  comments                 String?

  @@index([parentId])
  @@map("sub_weekly_cleaning_record_row")
}

model Sub_dispatch_cleaning_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  entranceDoorClosed       String?
  damagedDirtyBoxesPallets String?
  evidenceOfDust           String?
  palletsStackedOnEachOther String?
  loadingSpaceClean        String?
  noCansOnFloor            String?
  ventilationOk            String?
  floorsWallsClean         String?
  cleaningEquipmentStoredCorrectly String?
  correctiveActions        String?
  checkedBy                String?

  @@map("sub_dispatch_cleaning_inspection")
}

model Sub_facility_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  completedBy              String?

  rows Sub_facility_inspection_Row[]

  @@map("sub_facility_inspection")
}

model Sub_facility_inspection_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_facility_inspection @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  area                     String?
  checklist                String?
  findings                 String?

  @@index([parentId])
  @@map("sub_facility_inspection_row")
}

model Sub_facility_inspection_nc_report {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  completedBy              String?
  inspectionDate           String?
  managementVerification   String?
  verificationDate         String?

  rows Sub_facility_inspection_nc_report_Row[]

  @@map("sub_facility_inspection_nc_report")
}

model Sub_facility_inspection_nc_report_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_facility_inspection_nc_report @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  area                     String?
  nonConformance           String?
  correctiveAction         String?
  targetDate               String?
  actualDateCompleted      String?
  personResponsible        String?

  @@index([parentId])
  @@map("sub_facility_inspection_nc_report_row")
}

model Sub_chemical_stock_issue_register {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  nameOfChemical           String?
  approvedSupplier         String?
  batchNumber              String?
  msdsAvailable            String?
  expiryDate               String?
  dateOfIssue              String?
  quantityIssued           String?
  dilution                 String?
  issuedTo                 String?
  issuedBy                 String?
  areaToBeCleaned          String?

  @@map("sub_chemical_stock_issue_register")
}

model Sub_internal_pest_sightings_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  areaOfSighting           String?
  typeOfPest               String?
  reportedBy               String?
  carCompleted             String?
  pestControlCompanyNotified String?
  internalPestController   String?
  managementVerification   String?
  comment                  String?

  @@map("sub_internal_pest_sightings_log")
}

model Sub_pest_inspection_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  birdDroppingsSeen        String?
  birdDroppingsCorrectiveAction String?
  baitStationsObstructed   String?
  baitStationsCorrectiveAction String?
  inspectedBy              String?
  verifiedBy               String?

  rows Sub_pest_inspection_record_Row[]

  @@map("sub_pest_inspection_record")
}

model Sub_pest_inspection_record_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_pest_inspection_record @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  areaChecked              String?
  ratsMice                 String?
  flies                    String?
  cockroaches              String?
  birds                    String?
  ants                     String?
  moths                    String?
  other                    String?
  commentsCorrectiveAction String?

  @@index([parentId])
  @@map("sub_pest_inspection_record_row")
}

model Sub_staff_hygiene_inspection_weekends {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  verifiedBy               String?

  rows Sub_staff_hygiene_inspection_weekends_Row[]

  @@map("sub_staff_hygiene_inspection_weekends")
}

model Sub_staff_hygiene_inspection_weekends_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_staff_hygiene_inspection_weekends @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  staffMember              String?
  protectiveClothing       String?
  hairBootsClean           String?
  fingernails              String?
  noJewelleryMakeup        String?
  handWash                 String?
  injuryIllness            String?
  noFood                   String?
  correctiveAction         String?
  comments                 String?
  qcSignature              String?

  @@index([parentId])
  @@map("sub_staff_hygiene_inspection_weekends_row")
}

model Sub_staff_hygiene_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  weekStarting             String?
  shift                    String?
  verifiedBy               String?

  rows Sub_staff_hygiene_inspection_Row[]

  @@map("sub_staff_hygiene_inspection")
}

model Sub_staff_hygiene_inspection_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_staff_hygiene_inspection @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  staffMember              String?
  day                      String?
  protectiveClothing       String?
  hairBootsClean           String?
  fingernails              String?
  noJewelleryMakeup        String?
  handWash                 String?
  noFoodDrink              String?
  injuryIllness            String?
  comment                  String?
  qcSignature              String?

  @@index([parentId])
  @@map("sub_staff_hygiene_inspection_row")
}

model Sub_allergen_cleaning_verification {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  area                     String?
  equipmentSwabbed         String?
  allergensInvolved        String?
  productBefore            String?
  productAfter             String?
  debrisInspection         String?
  equipmentCleaned         String?
  correctBrushwareUse      String?
  protectiveClothingChanged String?
  handsWashedGlovesChanged String?
  partOfEquipmentSwabbed   String?
  swabResult               String?
  swabResult2nd            String?
  swabResult3rd            String?
  correctiveActionIfFailed String?
  completedBy              String?

  @@map("sub_allergen_cleaning_verification")
}

model Sub_daily_equipment_checklist {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  weekOf                   String?
  checkedBy                String?
  deviations               String?
  verifiedBy               String?

  rows Sub_daily_equipment_checklist_Row[]

  @@map("sub_daily_equipment_checklist")
}

model Sub_daily_equipment_checklist_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_daily_equipment_checklist @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  equipmentOrDevice        String?
  date                     String?
  condition                String?
  comments                 String?

  @@index([parentId])
  @@map("sub_daily_equipment_checklist_row")
}

model Sub_glass_plastic_equipment_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  weekOf                   String?
  inspectorInitials        String?
  verifiedBy               String?

  rows Sub_glass_plastic_equipment_inspection_Row[]

  @@map("sub_glass_plastic_equipment_inspection")
}

model Sub_glass_plastic_equipment_inspection_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_glass_plastic_equipment_inspection @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  areaChecked              String?
  day                      String?
  glassPlastics            String?
  equipment                String?
  comments                 String?

  @@index([parentId])
  @@map("sub_glass_plastic_equipment_inspection_row")
}

model Sub_glass_breakage_clearance_certificate {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  timeOfBreakage           String?
  areaOfBreakage           String?
  reportedBy               String?
  factoryManager           String?
  productsProcessed        String?
  finishedProductContaminated String?
  rawMaterialContaminated  String?
  packagingContaminated    String?
  machineEquipContaminated String?
  areaIsolated             String?
  areasEquipmentCleaned    String?
  clothingFootwearChecked  String?
  clothingContainedRemoved String?
  staffExcluded            String?
  cleaningEquipmentDiscarded String?
  minCleanersUsed          String?
  glassSampleRetained      String?
  allFragmentsRemoved      String?
  affectedMaterialIsolated String?
  areaInspectedAfterCleaning String?
  contaminatedProductDiscarded String?
  safeToRestart            String?
  timeOfRestart            String?
  carCompleted             String?
  carNo                    String?
  rootCauseIdentified      String?
  verifiedBy               String?

  rows Sub_glass_breakage_clearance_certificate_Row[]

  @@map("sub_glass_breakage_clearance_certificate")
}

model Sub_glass_breakage_clearance_certificate_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_glass_breakage_clearance_certificate @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  code                     String?
  productDescription       String?
  batchNo                  String?
  qty                      String?

  @@index([parentId])
  @@map("sub_glass_breakage_clearance_certificate_row")
}

model Sub_utensil_issue_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  shift                    String?
  correctiveActionComment  String?
  checkedBy                String?
  verifiedBy               String?

  rows Sub_utensil_issue_record_Row[]

  @@map("sub_utensil_issue_record")
}

model Sub_utensil_issue_record_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_utensil_issue_record @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  utensilType              String?
  qtyIssued                String?
  conditionBefore          String?
  qtyReturned              String?
  conditionAfter           String?
  unsatisfactoryDetail     String?

  @@index([parentId])
  @@map("sub_utensil_issue_record_row")
}

model Sub_safety_glass_register {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  weekStarting             String?
  shift                    String?
  verifiedBy               String?

  rows Sub_safety_glass_register_Row[]

  @@map("sub_safety_glass_register")
}

model Sub_safety_glass_register_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_safety_glass_register @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  staffMember              String?
  day                      String?
  issued                   String?
  returned                 String?
  comment                  String?
  signature                String?
  supervisor               String?
  qc                       String?

  @@index([parentId])
  @@map("sub_safety_glass_register_row")
}

model Sub_goggles_register {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  weekStarting             String?
  shift                    String?
  verifiedBy               String?

  rows Sub_goggles_register_Row[]

  @@map("sub_goggles_register")
}

model Sub_goggles_register_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_goggles_register @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  staffMember              String?
  day                      String?
  issued                   String?
  returned                 String?
  comment                  String?
  signature                String?
  qc                       String?

  @@index([parentId])
  @@map("sub_goggles_register_row")
}

model Sub_knife_register {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  weekStarting             String?
  shift                    String?
  verifiedBy               String?

  rows Sub_knife_register_Row[]

  @@map("sub_knife_register")
}

model Sub_knife_register_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_knife_register @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  staffMember              String?
  day                      String?
  issued                   String?
  returned                 String?
  comment                  String?
  signature                String?
  qc                       String?

  @@index([parentId])
  @@map("sub_knife_register_row")
}

model Sub_glove_issuing_register {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  personAllocated          String?
  quantityIssued           String?
  conditionWhenReturned    String?
  correctiveAction         String?
  signature                String?

  @@map("sub_glove_issuing_register")
}

model Sub_plaster_dressing_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  dateOfIncident           String?
  time                     String?
  itemIssued               String?
  nameOfInjured            String?
  descriptionOfInjury      String?
  firstAider               String?
  incidentRecordedNo       String?
  breakTime                String?
  lunchTime                String?
  knockOffTime             String?

  @@map("sub_plaster_dressing_inspection")
}

model Sub_first_aid_checklist_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  shift                    String?
  dateOfIncident           String?
  time                     String?
  itemIssued               String?
  intact                   String?
  cleanlinessOfDressing    String?
  firstAider               String?
  nameOfInjured            String?
  breakTime                String?

  @@map("sub_first_aid_checklist_record")
}

model Sub_salt_issuing_register {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  quantityIssued           String?
  batchNumber              String?
  issuedTo                 String?
  issuedBy                 String?

  @@map("sub_salt_issuing_register")
}

model Sub_chiller_batch_control {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  harvestFarm              String?
  processingStep           String?
  trackingNo               String?
  date                     String?
  time                     String?
  sizeRange                String?
  traysClearlyMarked       String?
  noOfTrays                String?
  chillerTemp              String?
  totalDaysInChiller       String?
  supervisor               String?

  @@map("sub_chiller_batch_control")
}

model Sub_dry_chiller_batch_control {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  harvestFarm              String?
  processingStep           String?
  date                     String?
  time                     String?
  traysClearlyMarked       String?
  noOfTrays                String?
  productTemp              String?
  chillerTemp              String?
  totalDaysInChiller       String?
  supervisor               String?
  factoryManager           String?

  @@map("sub_dry_chiller_batch_control")
}

model Sub_maintenance_job_card {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobCardNumber            String?
  maintenanceDetail        String?
  completedBy              String?
  issueDate                String?
  descriptionRequired      String?
  actualMaintenancePerformed String?
  timeStarted              String?
  timeFinished             String?
  technicianSignature      String?
  comments                 String?
  suitablyCarriedOut       String?
  nutsBoltsAccountedFor    String?
  areaCleanedAfter         String?
  managementVerification   String?
  verificationDate         String?

  @@map("sub_maintenance_job_card")
}

model Sub_water_tank_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  tankType                 String?
  filtersClean             String?
  tankCondition            String?
  tankSoiled               String?
  tankCleaned              String?
  reasonForCleaning        String?
  managementVerification   String?
  dateOfVerification       String?

  @@map("sub_water_tank_inspection")
}

model Sub_factory_maintenance_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  month                    String?
  date                     String?
  checkedBy                String?
  verifiedBy               String?

  rows Sub_factory_maintenance_inspection_Row[]

  @@map("sub_factory_maintenance_inspection")
}

model Sub_factory_maintenance_inspection_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_factory_maintenance_inspection @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  category                 String?
  equipment                String?
  inspected                String?
  commentsAction           String?

  @@index([parentId])
  @@map("sub_factory_maintenance_inspection_row")
}

model Sub_equipment_checklist_roof {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  checkedBy                String?
  verifiedBy               String?

  rows Sub_equipment_checklist_roof_Row[]

  @@map("sub_equipment_checklist_roof")
}

model Sub_equipment_checklist_roof_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_equipment_checklist_roof @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  component                String?
  description              String?
  inspected                String?
  commentsAction           String?

  @@index([parentId])
  @@map("sub_equipment_checklist_roof_row")
}

model Sub_daily_waste_removal {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  timeOfCollection         String?
  areaRemovedFrom          String?
  typeOfWaste              String?
  destinationDumpsite      String?
  dumpsiteRepSignature     String?
  vehicleRegNr             String?
  driver                   String?
  supervisor               String?

  @@map("sub_daily_waste_removal")
}

model Sub_boiler_inspection_report {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  timeBoilerStarted        String?
  waterLevelChecked        String?
  airReleaseValveOpened    String?
  burnerSwitchedOn         String?
  burnerLowFire            String?
  pressureChecked          String?
  mowbreyOpened            String?
  mainSteamValveOpened     String?
  timeBoilerTurnedOff      String?
  burnerAdjustedHighToLow  String?
  powerFailure             String?
  boilerRestarted          String?
  dieselReading            String?
  comments                 String?
  boilerOperator           String?

  @@map("sub_boiler_inspection_report")
}

model Sub_seamer_inspection_report {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  equipmentName            String?
  date                     String?
  preCleaningDone          String?
  compressorPressureChecked String?
  steamValveChecked        String?
  seamerMainRunDone        String?
  seamsChecked             String?
  chutesClean              String?
  noCansLidsFromPreviousShift String?
  beforeDeficiency         String?
  checkedByBefore          String?
  verifiedByBefore         String?
  managementBefore         String?
  cleaningOfSeamerDone     String?
  rollersLubricated        String?
  seamerCoverRemoved       String?
  oilLevelChecked          String?
  lidsEmptiedFromChute     String?
  cansLidsTakenToFG02      String?
  endDeficiency            String?
  checkedByEnd             String?
  verifiedByEnd            String?
  managementEnd            String?

  @@map("sub_seamer_inspection_report")
}

model Sub_incoming_ppe_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  product                  String?
  supplier                 String?
  approvedSupplier         String?
  deliveryNote             String?
  quantity                 String?
  carRequired              String?
  carNr                    String?
  receivedBy               String?

  @@map("sub_incoming_ppe_inspection")
}

model Sub_incoming_goods_inspection_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  product                  String?
  supplier                 String?
  approvedSupplier         String?
  deliveryNote             String?
  quantity                 String?
  carRequired              String?
  carNr                    String?
  receivedBy               String?

  @@map("sub_incoming_goods_inspection_log")
}

model Sub_cans_incoming_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  supplier                 String?
  descriptionOfProducts    String?
  quantityReceived         String?
  productionDate           String?
  productionTime           String?
  sampleSize               String?
  palletTrackingNo         String?
  vehicleClean             String?
  noPestActivity           String?
  noNonFoodTransported     String?
  noBrokenPalletsWetCans   String?
  cansFromApprovedSupplier String?
  qtyAccordingToOrder      String?
  coaReceived              String?
  seamOrFlangeDefects      String?
  dentedScratchesDust      String?
  rustedCans               String?
  foreignMatter            String?
  pestInfestationSigns     String?
  weldingDefects           String?
  otherIrregularities      String?
  cansAcceptedRejected     String?
  receivedInspectedBy      String?

  @@map("sub_cans_incoming_inspection")
}

model Sub_lids_incoming_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  supplier                 String?
  descriptionOfProducts    String?
  quantityReceived         String?
  sampleSize               String?
  palletTrackingNo         String?
  vehicleClean             String?
  noPestActivity           String?
  noNonFoodTransported     String?
  noBrokenPalletsWetCans   String?
  cansFromApprovedSupplier String?
  qtyAccordingToOrder      String?
  coaReceived              String?
  seamOrFlangeDefects      String?
  dentedScratchesDust      String?
  rustedCans               String?
  foreignMatter            String?
  pestInfestationSigns     String?
  weldingDefects           String?
  otherIrregularities      String?
  cansAcceptedRejected     String?
  receivedInspectedBy      String?

  @@map("sub_lids_incoming_inspection")
}

model Sub_lids_incoming_inspection_internal {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  supplier                 String?
  descriptionOfProducts    String?
  quantityReceived         String?
  sampleSize               String?
  palletTrackingNo         String?
  containerClean           String?
  noPestActivity           String?
  noNonFoodStored          String?
  noBrokenPalletsWetCans   String?
  containerCleanConfirmed  String?
  doorClosed               String?
  cansLidsFreeOfDustRust   String?
  qtyAccordingToOrder      String?
  coaReceived              String?
  seamOrFlangeDefects      String?
  dentedScratchesDust      String?
  rustedCans               String?
  foreignMatter            String?
  pestInfestationSigns     String?
  weldingDefects           String?
  otherIrregularities      String?
  cansAcceptedRejected     String?
  receivedInspectedBy      String?

  @@map("sub_lids_incoming_inspection_internal")
}

model Sub_incoming_goods_inspection {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  supplier                 String?
  productReceived          String?
  quantityReceived         String?
  batchNumber              String?
  expiryDate               String?
  coaReceived              String?
  sampleSize               String?
  fromApprovedSupplier     String?
  receivedAccordingToOrder String?
  vehicleConditionAcceptable String?
  pestSignsInVehicle       String?
  foreignObjectsRisk       String?
  contaminatingProductsTransported String?
  packagedLabelledCorrectly String?
  conformsToSpecification  String?
  otherConcerns            String?
  inspectedReceivedBy      String?

  @@map("sub_incoming_goods_inspection")
}

model Sub_dispatch_loading_inspection_checklist {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  clientDetails            String?
  noOfPalletsBoxes         String?
  deliveryNote             String?
  vehicleRegNumber         String?
  productDispatched        String?
  outerCartonSealed        String?
  damagedDirtyBoxesPallets String?
  damagedStockLoaded       String?
  pestInfestationStock     String?
  loadingSpaceClean        String?
  stockNotLoadedWithNonFood String?
  stockLoadedAsPerSalesOrder String?
  truckSecuredLocked       String?
  correctiveActions        String?
  checkedBy                String?

  @@map("sub_dispatch_loading_inspection_checklist")
}

model Sub_dispatch_receiving_checklist {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNumber                String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  clientDetails            String?
  noOfPalletsBoxes         String?
  deliveryNote             String?
  productDispatched        String?
  outerCartonSealed        String?
  damagedDirtyBoxesPallets String?
  damagedStockLoaded       String?
  pestInfestationStock     String?
  loadingSpaceClean        String?
  stockNotLoadedWithNonFood String?
  rustDentSigns            String?
  truckSecuredLocked       String?
  correctiveActions        String?
  checkedBy                String?

  @@map("sub_dispatch_receiving_checklist")
}

model Sub_water_monitoring {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  month                    String?
  date                     String?
  freshWaterPh             String?
  seaWaterSalt             String?
  seaWaterPh               String?
  checkedBy                String?
  comments                 String?

  @@map("sub_water_monitoring")
}

model Sub_lha_water_monitoring {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  month                    String?
  date                     String?
  shift                    String?
  timeOfCheck              String?
  chillerUnitOperating     String?
  temperature              String?
  checkedBy                String?

  @@map("sub_lha_water_monitoring")
}

model Sub_chiller_temperature_monitoring {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  jiReceivingDate          String?
  jiReceivedFrom           String?
  jiProcessingFor          String?
  jiIntakeWeight           String?
  date                     String?
  shift                    String?
  timeOfCheck              String?
  productCovered           String?
  chiller1DialTemp         String?
  chiller1ProductTemp      String?
  chiller2DialTemp         String?
  chiller2ProductTemp      String?
  freezerDialTemp          String?
  checkedBy                String?
  correctiveAction         String?

  @@map("sub_chiller_temperature_monitoring")
}

model Sub_incubator_temperature_check {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  shift                    String?
  startShiftTemp           String?
  lunchTemp                String?
  endShiftTemp             String?
  checkedBy                String?

  @@map("sub_incubator_temperature_check")
}

model Sub_dry_room_temp_humidity_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  week                     String?
  shift                    String?
  date                     String?
  room                     String?
  startShiftTemp           String?
  startShiftHumidity       String?
  duringProdTemp           String?
  duringProdHumidity       String?
  endShiftTemp             String?
  endShiftHumidity         String?
  checkedBy                String?
  correctiveAction         String?

  @@map("sub_dry_room_temp_humidity_log")
}

model Sub_grading_room_temp_humidity_log {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  week                     String?
  shift                    String?
  date                     String?
  room                     String?
  startShiftTemp           String?
  startShiftHumidity       String?
  duringProdTemp           String?
  duringProdHumidity       String?
  endShiftTemp             String?
  endShiftHumidity         String?
  checkedBy                String?
  correctiveAction         String?

  @@map("sub_grading_room_temp_humidity_log")
}

model Sub_internal_car {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  raisedBy                 String?
  raisedForArea            String?
  category                 String?
  investigationTeam        String?
  where                    String?
  who                      String?
  when                     String?
  visibleTrends            String?
  details                  String?
  immediateAction          String?
  rootCause                String?
  correctiveAction         String?
  verification             String?
  closedBy                 String?
  closeDate                String?
  verifiedBy               String?
  verifiedDate             String?

  @@map("sub_internal_car")
}

model Sub_disposition_investigation_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  productInvestigated      String?
  initiatedBy              String?
  date                     String?
  productionCode           String?
  assignedTo               String?
  reasonForInvestigation   String?
  reasonOtherSpecify       String?
  hazardsReducedAcceptable String?
  controlMeasuresEffective String?
  productStillMeetsLevel   String?
  isolatedToSingleItem     String?
  potentiallyUnsafe        String?
  allProductStillOnSite    String?
  analysisCanDemonstrateSafety String?
  clientsContactedForWithdrawal String?
  dispositionNecessary     String?
  quarantinedSegregated    String?
  dispositionDeemedNecessary String?
  methodOfDisposition      String?
  allPartiesNotified       String?
  approvedBy               String?
  dateReviewed             String?

  @@map("sub_disposition_investigation_record")
}

model Sub_withdrawal_mock_recall_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  chairedBy                String?
  apologies                String?
  typeOfRecall             String?
  reasonsForRecall         String?
  initiatedBy              String?
  productionCodePackingDate String?
  bestBeforeDate           String?
  dateTimeStarted          String?
  dateTimeFinished         String?
  amountStockProduced      String?
  amountDistributedStock   String?
  customersDistributedTo   String?
  amountCurrentlyInStock   String?
  amountAtCustomer         String?
  amountSoldByCustomer     String?
  actionsTaken             String?

  rows Sub_withdrawal_mock_recall_record_Row[]

  @@map("sub_withdrawal_mock_recall_record")
}

model Sub_withdrawal_mock_recall_record_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_withdrawal_mock_recall_record @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  nameSurname              String?
  department               String?
  openingSignature         String?
  closingSignature         String?

  @@index([parentId])
  @@map("sub_withdrawal_mock_recall_record_row")
}

model Sub_traceability {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  dateInitiated            String?
  startTime                String?
  finishTime               String?
  selectedItemDescription  String?
  itemType                 String?
  lotBatchNumber           String?
  totalAmountReceivedProduced String?
  receivingMatchesShipped  String?
  amountRawWarehouse       String?
  amountOnHold             String?
  amountInProcess          String?
  amountFinishedWarehouse  String?
  amountShipped            String?
  percentageAccountedFor   String?
  managementSignature      String?

  @@map("sub_traceability")
}

model Sub_traceability_mock_recall_canned_braised_abalone {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  date                     String?
  agCode                   String?

  rows Sub_traceability_mock_recall_canned_braised_abalone_Row[]

  @@map("sub_traceability_mock_recall_canned_braised_abalone")
}

model Sub_traceability_mock_recall_canned_braised_abalone_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_traceability_mock_recall_canned_braised_abalone @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  submissionRef            String?
  recordCode               String?
  recordName               String?
  summary                  String?
  included                 String?

  @@index([parentId])
  @@map("sub_traceability_mock_recall_canned_braised_abalone_row")
}

model Sub_traceability_mock_recall_canned_abalone {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  date                     String?
  agCode                   String?

  rows Sub_traceability_mock_recall_canned_abalone_Row[]

  @@map("sub_traceability_mock_recall_canned_abalone")
}

model Sub_traceability_mock_recall_canned_abalone_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_traceability_mock_recall_canned_abalone @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  submissionRef            String?
  recordCode               String?
  recordName               String?
  summary                  String?
  included                 String?

  @@index([parentId])
  @@map("sub_traceability_mock_recall_canned_abalone_row")
}

model Sub_traceability_mock_recall_canned_minced_abalone {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  date                     String?
  agCode                   String?

  rows Sub_traceability_mock_recall_canned_minced_abalone_Row[]

  @@map("sub_traceability_mock_recall_canned_minced_abalone")
}

model Sub_traceability_mock_recall_canned_minced_abalone_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_traceability_mock_recall_canned_minced_abalone @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  submissionRef            String?
  recordCode               String?
  recordName               String?
  summary                  String?
  included                 String?

  @@index([parentId])
  @@map("sub_traceability_mock_recall_canned_minced_abalone_row")
}

model Sub_traceability_mock_recall_dried_abalone {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  jobNo                    String?
  date                     String?
  agCode                   String?

  rows Sub_traceability_mock_recall_dried_abalone_Row[]

  @@map("sub_traceability_mock_recall_dried_abalone")
}

model Sub_traceability_mock_recall_dried_abalone_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_traceability_mock_recall_dried_abalone @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  submissionRef            String?
  recordCode               String?
  recordName               String?
  summary                  String?
  included                 String?

  @@index([parentId])
  @@map("sub_traceability_mock_recall_dried_abalone_row")
}

model Sub_traceability_mock_recall_live_abalone {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  recallInitiationDate     String?
  packingDate              String?

  rows Sub_traceability_mock_recall_live_abalone_Row[]

  @@map("sub_traceability_mock_recall_live_abalone")
}

model Sub_traceability_mock_recall_live_abalone_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_traceability_mock_recall_live_abalone @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  recordName               String?
  recordCode               String?
  summary                  String?
  included                 String?

  @@index([parentId])
  @@map("sub_traceability_mock_recall_live_abalone_row")
}

model Sub_supplier_questionnaire {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  companyName              String?
  companyAddress           String?
  emergencyContactName     String?
  emergencyContactNumbers  String?
  productsSupplied         String?
  foodSafetyContact        String?
  accountsContact          String?
  productionContact        String?
  technicalQualityContact  String?
  certificateOfAcceptability String?
  facilityCertified        String?
  certificationType        String?
  certificateDate          String?
  certificateAttached      String?
  writtenQualityPolicy     String?
  riskAssessmentUndertaken String?
  foodSafetySystem         String?
  cleaningProcedures       String?
  employeeHygieneProgram   String?
  personnelTrainedHygiene  String?
  pestControlProgram       String?
  crossContaminationControlled String?
  recallProgram            String?
  fullTraceability         String?
  complaintsHandlingSystem String?
  handlesAllergens         String?
  allergenControlProgram   String?
  supplierApprovalProgram  String?
  manufacturingInstructionsDocumented String?
  auditingCarriedOut       String?
  liabilityInsurance       String?
  otherFoodSafetyControls  String?
  completedByName          String?
  completedByPosition      String?
  completedByPhone         String?
  completedByDate          String?
  sqaApprovalName          String?
  sqaApprovalPosition      String?
  sqaApprovalDate          String?
  provisionalApproval      String?

  @@map("sub_supplier_questionnaire")
}

model Sub_supplier_approval_record {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  supplierName             String?
  productService           String?
  dateOfEvaluation         String?
  evaluator                String?
  evidenceOfCertificates   String?
  qualityCompliesSpec      String?
  deliveredOnTime          String?
  correspondsToOrder       String?
  documentationAdequate    String?
  technicalAssistanceAvailable String?
  totalPercent             String?
  actionIfNotApproved      String?
  managementVerification   String?
  verificationDate         String?

  @@map("sub_supplier_approval_record")
}

model Sub_rapid_food_handler_medicals {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  clinicSite               String?
  dateOfAssessment         String?
  nameOfFoodHandler        String?
  dobId                    String?
  chronicHT                String?
  chronicDM                String?
  chronicOther             String?
  urineProUroBlo           String?
  mdt                      String?
  bp                       String?
  hgt                      String?
  nearVision               String?
  scalp                    String?
  ears                     String?
  eyes                     String?
  nose                     String?
  mouthTeeth               String?
  handsNails               String?
  fitUnfit                 String?
  conditionsSpecsChronicControl String?
  comments                 String?
  medicalExaminer          String?
  practiceNo               String?
  signatureDate            String?

  @@map("sub_rapid_food_handler_medicals")
}

model Sub_rfh_medical_result {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  clinicSite               String?
  dateOfMedicalAssessment  String?
  nameOfFoodHandler        String?
  dobIdNumber              String?
  chronicMedicationUnderControl String?
  fitUnfit                 String?
  comments                 String?
  medicalExaminer          String?
  practiceNo               String?
  examinerDate             String?
  verifiedBy               String?
  verifiedDate             String?

  @@map("sub_rfh_medical_result")
}

model Sub_emergency_evacuation_attendance_register {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  buildingArea             String?
  typeOfDrill              String?
  timeStarted              String?
  timeEnded                String?
  notesComments            String?
  safetyOfficer            String?
  management               String?

  rows Sub_emergency_evacuation_attendance_register_Row[]

  @@map("sub_emergency_evacuation_attendance_register")
}

model Sub_emergency_evacuation_attendance_register_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_emergency_evacuation_attendance_register @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  name                     String?
  signature                String?

  @@index([parentId])
  @@map("sub_emergency_evacuation_attendance_register_row")
}

model Sub_handling_of_emergencies_and_incidences {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  typeOfDrill              String?
  timeEvacuating           String?
  timeReturning            String?
  durationOfDrill          String?
  durationComments         String?
  durationDeviation        String?
  chillerTempAfterDrill    String?
  chillerTempComments      String?
  chillerTempDeviation     String?
  tableTempAfterDrill      String?
  tableTempComments        String?
  tableTempDeviation       String?
  foreignMatterSigns       String?
  foreignMatterComments    String?
  dryCookingWithinSpec     String?
  dryCookingComments       String?
  retortReportWithinSpec   String?
  retortReportComments     String?
  hygieneInspectionAfterDrill String?
  hygieneInspectionComments String?
  ppeChangedAfterEvacuation String?
  ppeChangedComments       String?
  completedBy              String?
  completedDate            String?
  verifiedBy               String?
  verifiedDate             String?

  @@map("sub_handling_of_emergencies_and_incidences")
}

model Sub_daily_factory_feedback_meeting {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  date                     String?
  intakes                  String?
  adherenceToPreviousPlan  String?
  planForTheDay            String?
  productToBeCannedToday   String?
  cleaningConforming       String?
  noOfDamages              String?
  productExceeding2DaysInChiller String?
  ingoConcerns             String?
  drainWeightsPreviousDay  String?
  seamProblem              String?
  printingMisprints        String?
  cansOutOfSpec            String?
  absenteeismSickLeave     String?
  wasteManagement          String?

  rows Sub_daily_factory_feedback_meeting_Row[]

  @@map("sub_daily_factory_feedback_meeting")
}

model Sub_daily_factory_feedback_meeting_Row {
  id       Int     @id @default(autoincrement())
  parentId String
  parent   Sub_daily_factory_feedback_meeting @relation(fields: [parentId], references: [id], onDelete: Cascade)
  position Int     @default(0)

  nameSurname              String?
  present                  String?

  @@index([parentId])
  @@map("sub_daily_factory_feedback_meeting_row")
}

model Sub_rtw_medical_questionnaire {
  id          String    @id
  status      String?
  source      String?
  submittedAt DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  rawJson     String?
  inSpec      Boolean?

  employeeNr               String?
  skinRashes               String?
  skinRashesRemarks        String?
  gumsThroatMouthDisease   String?
  gumsThroatMouthRemarks   String?
  diarrhoeaVomiting        String?
  diarrhoeaVomitingRemarks String?
  earsNoseEyesDischarge    String?
  earsNoseEyesRemarks      String?
  coldsCoughsFever         String?
  coldsCoughsFeverRemarks  String?
  employeeSignature        String?
  employeeSignatureDate    String?
  supervisorSignature      String?

  @@map("sub_rtw_medical_questionnaire")
}
