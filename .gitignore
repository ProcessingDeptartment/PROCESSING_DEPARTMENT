# dependencies
node_modules/

# environment
.env
.env.local
.env.*.local

# build output
dist/
build/
out/
coverage/

# Prisma
prisma/dev.db
prisma/dev.db-journal

# editor / OS
.vscode/
.idea/
.DS_Store
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Hidden artifacts the T: network share drops next to the record files --
# byte copies of records, invisible to ls but visible to git. Existing ones are
# relocated to junk/; this stays as a safety net in case the share drops more
# directly into public/records/.
public/records/.*
junk/
